<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <title>Doctor's Dashboard</title>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar {
            background-color: #007bff;
        }
        .navbar-nav {
            flex-direction: column; /* Make nav items stack vertically */
            float: left; /* Float navbar items to the left */
        }
        .navbar-brand, .nav-link {
            color: white;
        }
        .nav-link:hover {
            background-color: #0056b3;
            border-radius: 5px;
        }
        h1 {
            color: #007bff;
        }
        #content {
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .button-group {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Moseh Telemedic</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="patient-requests"><i class="bi bi-file-earmark-text"></i> Check Patient Requests</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="schedule-meeting"><i class="bi bi-calendar-plus"></i> Schedule Meeting</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="discharge-patient"><i class="bi bi-check-circle"></i> Discharge Patient</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="cancel-meeting"><i class="bi bi-x-circle"></i> Cancel Meeting</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="update-profile"><i class="bi bi-person-fill"></i> Update Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-action="manage-appointments"><i class="bi bi-calendar-check"></i> Manage Appointments</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1 class="text-center">Welcome, Dr. [Doctor's Name]</h1>
        <p class="text-center">Use the navigation bar to manage your appointments and patient requests.</p>

        <div id="content">
            <h2 class="text-center">Select an option from the menu</h2>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script>
        // Dynamic content loading based on navigation
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', function (event) {
                event.preventDefault(); // Prevent default link behavior
                const action = this.getAttribute('data-action');

                // Load corresponding content
                if (action === "patient-requests") {
                    loadPatientRequests();
                } else if (action === "schedule-meeting") {
                    scheduleMeeting();
                } else if (action === "discharge-patient") {
                    dischargePatient();
                } else if (action === "cancel-meeting") {
                    cancelMeeting();
                } else if (action === "update-profile") {
                    updateProfile();
                } else if (action === "manage-appointments") {
                    manageAppointments();
                }
            });
        });

        function loadPatientRequests() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function scheduleMeeting() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function dischargePatient() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function cancelMeeting() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function updateProfile() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function manageAppointments() {
            document.getElementById('content').innerHTML = `...
                ... (existing content for this function)
            `;
        }

        function approveRequest() {
            alert('Patient request approved!');
        }

        function rejectRequest() {
            alert('Patient request rejected!');
        }
    </script>
</body>
</html>
